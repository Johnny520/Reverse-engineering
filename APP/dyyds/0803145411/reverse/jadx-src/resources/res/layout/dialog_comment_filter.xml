<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:padding="20dp"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">
    <TextView
        android:textSize="14sp"
        android:textColor="@color/secondary_text"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="12dp"
        android:text="包含这些关键词的评论将被隐藏，一行一个关键词"/>
    <EditText
        android:textSize="14sp"
        android:textColor="@color/primary_text"
        android:textColorHint="@color/hint_text"
        android:gravity="start|top"
        android:id="@+id/filterKeywordsEditText"
        android:background="@drawable/round_edit_bg"
        android:paddingTop="12dp"
        android:paddingBottom="12dp"
        android:scrollbars="vertical"
        android:layout_width="match_parent"
        android:layout_height="200dp"
        android:hint="一行一个关键词\10例如:\10广告\10垃圾\10恶意"
        android:inputType="textMultiLine"
        android:paddingStart="12dp"
        android:paddingEnd="12dp"/>
    <TextView
        android:textSize="12sp"
        android:textColor="@color/secondary_text"
        android:gravity="end"
        android:id="@+id/filterCountText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:text="已设置 0 个关键词"/>
</LinearLayout>
