<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:paddingTop="2dp"
    android:paddingBottom="16dp"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:paddingStart="18dp"
    android:paddingEnd="18dp">
    <LinearLayout
        android:gravity="center_vertical"
        android:orientation="horizontal"
        android:background="@drawable/bg_batch_summary"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingHorizontal="12dp"
        android:paddingVertical="10dp">
        <TextView
            android:textSize="13sp"
            android:textColor="@color/text_secondary"
            android:id="@+id/batch_count_text"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:includeFontPadding="false"
            android:layout_weight="1"/>
        <TextView
            android:textSize="13sp"
            android:textStyle="bold"
            android:textColor="@color/xhs_red"
            android:gravity="center"
            android:id="@+id/select_all_btn"
            android:background="@drawable/bg_batch_select_all_btn"
            android:focusable="true"
            android:clickable="true"
            android:layout_width="wrap_content"
            android:layout_height="30dp"
            android:minWidth="58dp"
            android:includeFontPadding="false"
            android:paddingHorizontal="12dp"/>
    </LinearLayout>
    <ScrollView
        android:id="@+id/batch_image_scroll"
        android:scrollbars="none"
        android:fadingEdgeLength="0dp"
        android:clipToPadding="false"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="12dp"
        android:overScrollMode="never">
        <GridLayout
            android:id="@+id/batch_image_grid"
            android:background="@drawable/bg_batch_grid"
            android:padding="6dp"
            android:clipToPadding="false"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:columnCount="3"/>
    </ScrollView>
    <TextView
        android:textSize="15sp"
        android:textStyle="bold"
        android:textColor="@color/text_white"
        android:gravity="center"
        android:id="@+id/download_selected_btn"
        android:background="@drawable/bg_btn_download"
        android:focusable="true"
        android:clickable="true"
        android:layout_width="match_parent"
        android:layout_height="44dp"
        android:layout_marginTop="14dp"
        android:includeFontPadding="false"/>
</LinearLayout>
