#import "WCLGColorPickerBackgroundView.h"

/*
 * 这是依据 Objective-C runtime 元数据恢复的可读接口骨架。
 * 每个方法均保留原始 IMP 地址；精确指令位于 04_exact_arm64_assembly。
 */

@implementation WCLGColorPickerBackgroundView

+ (Class)layerClass
{
    // 原始 IMP: 0x249B1C; ObjC type encoding: #16@0:8
    // 已去除符号名混淆；完整控制流参见同名类的 .s 文件。
    return nil;
}

- (void)layoutSubviews
{
    // 原始 IMP: 0x249C8C; ObjC type encoding: v16@0:8
    // 已去除符号名混淆；完整控制流参见同名类的 .s 文件。
}

@end
